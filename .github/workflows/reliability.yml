name: Sustained reliability

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  soak:
    uses: batchstream/sink-production-suite/.github/workflows/reliability.yml@4ce25596bddbcf743d45421af00845a64aef8a80
    with:
      suite_ref: 4ce25596bddbcf743d45421af00845a64aef8a80
      sink_ref: ${{ github.sha }}
